/*#include "Algorytmy.h"
#include "Testowanie.h"

void BubbleSort(float tab[], int size)
{
	while (!CzyPosortowane(tab, size))
	{
		for (int j = 0; j < size - 1; j++)
		{
			if (tab[j] > tab[j + 1])
			{
				float a;
				a = tab[j];
				tab[j] = tab[j + 1];
				tab[j + 1] = a;
			}
		}
	}
}
*/